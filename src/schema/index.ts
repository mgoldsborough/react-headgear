/**
 * Schema builders for JSON-LD structured data.
 *
 * @example
 * ```typescript
 * import { Product, Article, BreadcrumbList } from 'react-headgear/schema';
 *
 * const productSchema = Product({
 *   name: 'Widget Pro',
 *   price: 29.99,
 *   currency: 'USD',
 * });
 * ```
 *
 * @module react-headgear/schema
 */

export * from './types';
export * from './builders';
